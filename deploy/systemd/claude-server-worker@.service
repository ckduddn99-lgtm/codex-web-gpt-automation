[Unit]
Description=Claude Code worker for the sealed AI bus
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=%i
Environment=HOME=/home/%i
Environment=PATH=/home/%i/.local/bin:/snap/bin:/usr/local/bin:/usr/bin:/bin
ExecStart=/usr/bin/python3 /home/%i/codex-web-gpt-automation/bin/cli_server_worker.py --db /home/%i/.local/state/ai-bus/bus.sqlite3 --provider claude --recipient claude --worker-id claude-code --cli /home/%i/.local/bin/claude --serve --interval 5
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
